#include<stdio.h>
#include<stdbool.h>

bool is_tri_num(int num);
inline int sumNatNumsEuler (int num);
int main (int argc, char** argv){
  // kalla a fall
};

inline int sumNatNumsEuler (int n){
  return (n*(n+1))/2;
};

bool is_tri_num(int num){
  size_t a = 0;
  while(1){
   sumNatNumsEuler(int a); 
  }
}
