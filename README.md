# About
# Usage
run `make all` in your terminal. This yields 2 executables, one from each implementations.
to use the C one:
```sh
./build/tri_num_c 21
```
and the Haskell one:
```sh
./build/tri_num_hs 21
```
